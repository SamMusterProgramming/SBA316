


//set up the icons on the sidebar
const iconContainer = document.getElementById('icon')
const icons = icon.querySelectorAll('div')
Array.from(icons, (icon) => {
    icon.style.fontSize ="18px"
    icon.style.marginLeft = "25px"
})

